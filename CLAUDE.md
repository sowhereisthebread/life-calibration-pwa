# CLAUDE.md — Tako 開機規則

> Claude Code 啟動時讀取。本檔須能在本機與 Cloud repo 單獨成立；並與 `AGENTS.md` 保持語義同步。

## 優先序

**Tako 當下的明確指示 ＞ 該專案正典與規則 ＞ 當前工作區／本輪已驗證的證據 ＞ 記憶、舊檔與歷史紀錄。**

## 核心守則

1. 全程使用繁體中文（台灣正體）。回應、檔案、表格與程式碼註解送出前均須自檢。
2. 回覆與施工前先對齊 Tako 當前真正的需求；精確事實回看原檔、紀錄與實際狀態，涉及時間先查實際時間，可查證的外部現況用適當工具與一手來源確認，不憑印象補數字或結論。
3. AI 可提出選項、反例、異議與檢查，但方向、權衡、高風險裁決與正式驗收由 Tako 決定；AI 的「approved」、建議或多模型共識都不等於授權。
4. 低風險、可逆操作可連續完成多步；高風險、不可逆、易誤用或複雜工作才使用 Plan／分步確認。若疑義會影響範圍、安全或資料完整性，停止並請 Tako 裁決。
5. 長期記憶只保留穩定、跨專案仍有用的背景；專案規則、階段、版本、實作狀態與檔案清單留在專案文件，不提升成跨專案記憶或規則。

## 開工順序

1. 讀完本檔。
2. 讀該專案入口／架構正典與本任務需要的執行規則。歷史檔與 `_archive/` 只有追溯需要時才讀，且不得當作現行規則來源。
3. 若工作區存在 `AI協作與工程守則_v8.md`，遇到本機環境、多檔案、同步／清理或證據寫入任務時讀取相關段落；Cloud repo 不存在該檔時不視為缺失依賴。
4. 不假設 repo／當前工作區以外的本機路徑、設定或檔案在 Cloud 也存在；以當前環境實際可見內容為準。

## 施工規則

- 任務交接已提供且仍有效的驗證結果，不重新探索；只有狀態變更、證據不足或發現矛盾時才重查。
- 動檔前確認施工範圍、會動到的檔案、回退方式與驗收條件。
- 不動本次任務以外的內容。遇到 Tako 手動修改、其他執行者變更或非預期未提交內容時，不自行回復、清理或覆蓋，先停止並回報。
- 刪檔、破壞性 Git、同步或大規模清理前，先確認正確 repo、工作樹與可回復範圍；未獲明確授權且未確認可回復方式時不得執行。
- 能實際驗證就不只靜態推測；程式、腳本、HTML 或可執行產物應跑相應測試，不能執行時明示限制。
- 同一條規則只保留一份權威版本；搬動規則時退役舊位置或留下路由。`CLAUDE.md` 與 `AGENTS.md` 這對開機檔例外，但內容必須保持語義同步。

## 完成的定義

- [ ] 已完成本輪核准範圍，未自行擴張；需要落檔的產出已在正式位置。
- [ ] 已執行適用的測試、差異比對或實際驗證；無法執行者已明示。
- [ ] 權限提示、工具中斷、部分輸出或對話結束都不算完成；已從最後驗證狀態續作到交付條件成立。
- [ ] 回報先給白話結論，再附路徑、diff、SHA、測試等實際證據，並列出未解項目與 `#待補`。
- [ ] 若更動檔名、路徑或結構，只同步更新實際引用到它的入口、索引或規則文件。
- [ ] 需要正式驗收的任務，施工者未自行宣告通過；全文繁體中文抽查完成。

規則仍有衝突時，依本檔優先序判斷；若仍無法解決且會影響範圍、安全或資料完整性，停止並請 Tako 裁決。
