# SPEC.md 併檔逐節對應表

產出日期：2026-08-06｜批次 C-4
對象：`SPEC.md`（v2，342 行、20,190 bytes，13 個標題區塊）→ 併入 `../../TAKO/TAKO_架構.md`，原檔以 `git mv` 封存至本目錄，SHA-256 未變：`641f4cd7fd7c308c08414fa4a81f924ab10bef223f44b82555e43d1e6d13864e`。

> 批次指示寫「九節」，實際檔案有 **十一個編號節**（一～十一），另加無編號的檔首前言與〈適用範圍與權限〉，
> 共 13 個標題區塊。本表涵蓋全部 13 個，不是 9 個。

## 對應表

| # | 節 | 行 | 處置 | 理由 |
|---|---|---|---|---|
| 0 | 檔首前言 | 1–15 | **拆分**：六輪 TURN 說明與 `support.js` 註記 → HANDOFF 第 0 節；「衝突時以基準為準」→ 丟棄 | 前者是「基準檔怎麼讀」的操作知識，該跟著基準走，而基準已於 C-5 移出工作目錄，接手者更需要它；後者架構.md 檔頭第 6 行已有同義句 |
| 1 | 適用範圍與權限 | 19–24 | **保留但移往他處**：HANDOFF | 是某一次批次的授權邊界紀錄，不是架構條款也不是現況描述。HANDOFF 5.5 節已有等價敘述 |
| 2 | 一、設計前提 | 27–47 | **部分併入架構.md 第五章**（變數命名 INK→ACT 一句）；其餘丟棄 | 三條總則（光澤不填字／最多三層／色碼要量過）架構.md 第五章已逐條有；三個「已診斷的問題」是舊值域的病歷，決定本身已寫進架構.md，病歷由 git history 承擔 |
| 3 | 二、Token 表 | 50–129 | **部分併入架構.md 第五章**（`--shadow-card`、版面節奏三個 token 與 `.app-shell` padding、「畫面下半部不要讓文字躺在裸底上」）；其餘丟棄 | 2.1／2.2／2.3 與架構.md 色票表重複；2.4 字體與「字體與圖示」重複；2.2 的實測數字已被 C-3 新條款（取最暗合成背景的最差值）推翻，且本身是單點量測值 |
| 4 | 三、材質層與元件對應 | 133–209 | **部分併入架構.md 第五章**（ICE 判準一句）；3.2 選擇器表與 3.3 材質 CSS 丟棄 | 3.2／3.3 是 `style.css` 的鏡像，且**已證實漂移**：3.2 寫「`.metric` 在 GROUND 上是 ICE、卡片內是 GLASS」，實際 `style.css:241` 的 `.metric` 無條件屬 GLASS，`.card .metric` 只剩 `:1061` 一條顏色規則。依架構.md 第六章「同一件事只保留一份有效版本」，材質對應由 `style.css` 單獨承擔 |
| 5 | 四、尺寸階層 | 213–237 | **丟棄** | 四階表、「尺寸與材質是兩個獨立維度」、觸控區補到 44px、狀態指示不是 chip，架構.md 第五章「材質分層與尺寸階層」四段已全數涵蓋。兩行觸控區 CSS 是 code 副本 |
| 6 | 五、中英文規則 | 241–265 | **丟棄** | 整節前提（英文只有兩種用途）已被 C-2 推翻，架構.md 語言段改為「介面預設英文，例外用中文」。「已完成的字串」表是改版過程紀錄，屬 git history |
| 7 | 六、`app.js` 的改動範圍 | 269–280 | **丟棄** | 是單一批次的施工紀錄，HANDOFF 第 1 節已有逐項等價內容（`app.js` 四處、`data-core.js` 的 `formatMinutes()`） |
| 8 | 七、基準有、現行仍未實作 | 284–296 | **保留但移往他處**：HANDOFF 未處理清單 | 是待辦，不是規格。九項中六項已於批次 A／A-補完成，僅剩目標 chip 千分位、chip 28px 階、完整中英文盤點 |
| 9 | 八、基準未涵蓋、依材質對應推導 | 300–311 | **丟棄** | 六條推導結果已分別落在架構.md 第五章（獨立區段→ICE、小格→GLASS、卡內列→無材質、可填→DEBOSS）與第二章（THIS MONTH 維持高亮）。推導過程本身不需保留 |
| 10 | 九、停下來回報的項目 | 315–320 | **保留但移往他處**：HANDOFF 未處理清單 | 四項都是待 Tako 裁決的開放問題，與 HANDOFF 第 3 節同一批，合併去重 |
| 11 | 十、已驗證的結論 | 324–334 | **保留但移往他處**：HANDOFF；其中 `--ground-solid` 一條丟棄 | 這是「試過、失敗、不要再試」的負面知識 —— 失敗的嘗試不會進 commit，git history 救不回來，是本檔最不可替代的一節。`--ground-solid` 該條已隨 b331e8c 移除填色圓而失效，C-3 亦已刪除該 token |
| 12 | 十一、尚未定案 | 338–342 | **保留但移往他處**：HANDOFF 未處理清單 | 同 #10 |

## 併入架構.md 的六處（逐條）

1. 第五章「材質分層」— 補基準 TURN 5 寫 `05 · INK — 動作`，與 `05 ACT` 同一層（來源：SPEC 一、變數命名）
2. 第五章「材質分層」— 補 ICE 的判準「內容會從它下面滑過去、需要被糊掉」（來源：SPEC 3.1）
3. 第五章色票表 — 補 `--shadow-card`（來源：SPEC 2.1）
4. 第五章圓角行之後 — 補版面節奏三個 token 與 `.app-shell` padding（來源：SPEC 2.5）
5. 第五章 — 補「畫面下半部不要讓文字直接躺在裸底上」（來源：SPEC 2.2 使用規則第 1 條）
6. 檔頭第 6 行 — 順位由三層改兩層（SPEC.md 消失）

## 沒有搬到任何地方的內容

見批次 C 完工回報的「被丟棄原文」段，逐段附原文與行號。
