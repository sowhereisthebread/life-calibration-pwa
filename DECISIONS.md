# 人生主控表・校準版 v0.3.4 決策紀錄

這個 App 現在可以在手機或電腦快速記帳、管理義務與專案、記錄睡眠和工作，並做七天回顧與每月結算；資料只留在使用它的瀏覽器。
v0.3.4 為 MONEY／WORK／PROJECTS／REVIEW／DATA 五分頁，舊 v0.2 資料會自動升級，儲存鍵與正式網址不變。
v0.3.4 已於 2026-08-03 推送並部署至既有 `master`／`/(root)` GitHub Pages，iPhone 實機驗收已通過；部署與安裝指引保留在 `DEPLOY.md`。
目前待 Tako 決定的事項只剩「是否跨裝置同步」；換網址、瀏覽器或裝置前仍要先匯出 JSON。

## 已採用的決策

### 1. 每天只保留一個簡單入口（已由第 21 項取代）
我決定了什麼：預設永遠打開「今天」，睡眠、工作、收支、恢復與備註都在同一條往下滑的路徑。  
為什麼：每天不需要先找功能，手機拿起來就能記。  
不這樣做會怎樣：資料分散在多層選單，容易重演 Excel 不夠直覺的問題。  
改動成本：要花點時間（要重做一部分）。

### 2. 用底部四分頁（已由第 21 項取代）
我決定了什麼：固定使用「今天／專案／回顧／資料」四個底部分頁。  
為什麼：四個用途彼此清楚，且都在拇指容易碰到的位置。  
不這樣做會怎樣：若把資料功能藏進選單，備份提醒會更容易被忽略。  
改動成本：要花點時間（要重做一部分）。

### 3. 所有日常欄位輸入即存
我決定了什麼：時間、文字、效果與修改中的資料都會立即存入，不另設「儲存」按鈕。  
為什麼：減少一次操作，也避免填完後忘記按儲存。  
不這樣做會怎樣：使用者可能誤以為已完成，關閉後才發現資料沒留下。  
改動成本：好改（十分鐘內）。

### 4. 每日資料以日期收在同一包
我決定了什麼：所有資料放在瀏覽器的一包資料裡，每天用 `年-月-日` 當索引，並附上資料版本。  
為什麼：這讓匯出、還原與未來改版都有清楚界線。  
不這樣做會怎樣：資料散落時，備份可能漏掉一部分，改版也較難安全轉換。  
改動成本：難改（等於重來）。

### 5. 睡眠自動處理跨午夜
我決定了什麼：就寢時間晚於起床時間時，一律視為睡到隔天，例如 23:30 到 07:00 算 7 小時 30 分。  
為什麼：一般睡眠多半跨日，不應要求使用者另外選日期。  
不這樣做會怎樣：睡眠會出現負數，或需要多填一個不直覺的日期。  
改動成本：好改（十分鐘內）。

### 6. 未完成的工作段不計工時
我決定了什麼：只有上班、沒有下班的工作段會標示「未完成」，並暫時不加入總工時。  
為什麼：用零或猜測的結束時間都會讓回顧數字失真。  
不這樣做會怎樣：忘記下班打卡時，當日與七日工時可能變成錯誤的大數字。  
改動成本：好改（十分鐘內）。

### 7. 收支預設為支出與餐飲
我決定了什麼：新增收支時預設選「支出」，分類先放「餐飲」，同時允許直接輸入自訂分類。  
為什麼：日常最常記的是支出，先給常用值能縮短操作時間。  
不這樣做會怎樣：每一筆都要從空白開始，60 秒目標更難達成。  
改動成本：已由第 17 項取代。

### 8. 專案只保留名稱、狀態與下一步
我決定了什麼：專案不加入子任務、期限或複雜分類，只顯示能立刻執行的下一步。  
為什麼：這頁的工作是早上看一眼就知道要做什麼，不是管理所有工作。  
不這樣做會怎樣：專案頁會逐漸變成另一套需要維護的任務系統。  
改動成本：要花點時間（要重做一部分）。

### 9. 專案排序先看狀態，再看更新時間
我決定了什麼：進行中排最上面，同狀態內最近更新的排前面，完成項目放最後。  
為什麼：最可能要做的事情會自然出現在視線最前方。  
不這樣做會怎樣：早上仍要自己從清單裡找出真正要推進的項目。  
改動成本：好改（十分鐘內）。

### 10. 回顧只用文字與數字（已由第 22 項取代）
我決定了什麼：最近七天逐日列出睡眠、工時、收支淨額與恢復效果，上方只放四個摘要數字。  
為什麼：七天校準期需要的是看懂現況，不需要圖表裝飾或複雜分析。  
不這樣做會怎樣：畫面更擠，還會把時間花在解讀視覺效果而不是看資料。  
改動成本：要花點時間（要重做一部分）。

### 11. 匯入採完整取代並先自動備份
我決定了什麼：匯入前檢查版本、要求兩次確認，並先自動下載目前資料，再整包取代。  
為什麼：合併規則容易產生重複或衝突；先備份則保留反悔機會。  
不這樣做會怎樣：匯入失誤可能覆蓋唯一一份資料，或把同一天內容重複加入。  
改動成本：要花點時間（要重做一部分）。

### 12. 不同開啟方式視為不同資料庫
我決定了什麼：App 明確提醒固定使用同一台裝置、同一個網址與同一個瀏覽器；雙擊改成伺服器模式時資料不會自動搬移，電腦與手機也不會同步。

為什麼：瀏覽器會把不同網址分開保存，這不是 App 能在本輪改變的限制。

不這樣做會怎樣：換個網址後看到空白頁，容易誤以為原資料消失。

改動成本：難改（等於重來）。

### 13. 每七天提醒匯出，不保證瀏覽器保存期限
我決定了什麼：有資料且距離建立或上次匯出滿七天時顯示備份提醒，不宣稱瀏覽器一定在哪一天刪除。

為什麼：瀏覽器（特別是 iOS Safari）可能依儲存政策、使用頻率及裝置空間清理網站的本機資料，未安裝到主畫面的網站風險較值得注意；不保證固定天數。

不這樣做會怎樣：使用者可能把提醒期限誤認成安全保證，直到資料遭清理才發現沒有備份。

改動成本：好改（十分鐘內）。

### 14. PWA 已進入 GitHub Pages 與 iOS 安裝階段
我決定了什麼：PWA 的相對路徑、安裝資訊、離線程式與 iOS meta 已備妥；正式 HTTPS 部署與 iPhone 加入主畫面由 Tako 依 `DEPLOY.md` 完成。
為什麼：iOS 必須從正式 HTTPS 網址加入主畫面，才能固定以 standalone 版本使用；本機不能代替實機與 GitHub 帳號操作。
不這樣做會怎樣：可能把本機設定完成誤報成已在 iPhone 實際安裝，或長期從 Safari 分頁記錄而增加資料遭清理的風險。
改動成本：好改（十分鐘內）。

### 15. Git 作者只設定在這個工作區
我決定了什麼：因這台電腦沒有 Git 作者資料，只在本儲存庫使用 `Tako <tako@local>`，不改全機設定。  
為什麼：任務要求分階段提交，但不應替 Tako 猜測公開信箱或影響其他專案。  
不這樣做會怎樣：無法提交，或其他專案可能被套用不正確的作者資料。  
改動成本：好改（十分鐘內）。

### 16. 啟動腳本只使用已存在的 Python
我決定了什麼：Windows 依序嘗試 PATH 中的 `python`、可正常執行的 `py -3`，再尋找使用者目錄中既有的 Python；macOS／Linux 只使用可正常執行的 `python3` 或 `python`。找不到就清楚停止，不自行下載工具。

為什麼：自動下載會連上外部服務，也讓本來單純的本機工具多出未知套件。

不這樣做會怎樣：第一次啟動可能在使用者不知情時連網，或因下載失敗而留下難懂錯誤。

改動成本：好改（十分鐘內）。

### 17. 收支預設為支出與生活，記住最近自訂分類（已由第 23 項更新）
我決定了什麼：新增收支預設選「支出」與「生活」；預設分類依序為生活、餐飲、交通、居家、醫療、其他，使用自訂分類後，下次優先帶入最近一次使用的自訂分類。
為什麼：常用生活支出會因實際使用改變，最近使用值比固定預設更省操作。
不這樣做會怎樣：常用支出每次都要重選或重打分類，增加記錄摩擦。
改動成本：好改（十分鐘內）。

### 18. 七天備份提醒改為可略過橫幅
我決定了什麼：備份到期時顯示不阻擋操作的橫幅，可按「本次略過」讓當天不再出現；隔天重新提醒，成功匯出後立即消失並重算七天。
為什麼：保留備份節奏，同時避免每次開啟都重複打擾。
不這樣做會怎樣：彈窗或反覆提醒會阻礙快速記錄，使用者也可能因此忽略真正重要的備份。
改動成本：好改（十分鐘內）。

### 19. 本機資料讀寫集中在獨立模組
我決定了什麼：UI 不直接碰 localStorage，統一透過資料模組讀取當日、寫入欄位、增刪改工作／收支／專案，以及匯出、匯入與清除。
為什麼：資料規則有單一入口後，介面修改不必同時拆解儲存細節，也能獨立測試。
不這樣做會怎樣：輸入即存會持續與畫面事件綁在一起，後續小改也容易造成資料回歸。
改動成本：要花點時間（需調整資料模組與測試，但不必重做 UI）。

### 20. 正式部署採獨立 public repo 的 GitHub Pages
我決定了什麼：`life-calibration/` 是獨立 repo，只發布 App 程式與文件；Pages 從 repo 根目錄啟動，正式網址為 `<帳號>.github.io/<repo>/`，所有資源、manifest 與 Service Worker 都使用相對路徑。
為什麼：獨立 public repo 可讓 GitHub Free 直接使用 Pages，同時不會把整個知識庫工作區或個人資料推上 GitHub。
不這樣做會怎樣：若從上層工作區發布，交易系統、產業鏈及其他私人檔案可能一起外流；若沿用舊網址格式，則會多出不存在的 `/life-calibration/`。
改動成本：要花點時間（需重新部署與覆驗 repo 根路徑）。

### 21. 五分頁重排取代單一今天入口與四分頁
我決定了什麼：底部固定為 MONEY／WORK／PROJECTS／REVIEW／DATA，預設 MONEY；既有睡眠、打卡、恢復、備註與專案行為保留，只搬到對應頁面。

為什麼：記帳與待辦已成為每天的主要入口，五頁能讓金錢、日常、承諾、回顧與資料管理各自清楚，同時維持拇指可及。

不這樣做會怎樣：所有功能擠在「今天」會越來越長，重要的記帳與到期事項也不容易一開站就看見。

改動成本：要花點時間（需同步調整導覽、版面與驗收流程）。

### 22. MONEY 使用純 SVG 圓餅圖，取代回顧完全無圖表
我決定了什麼：最近七天仍保留文字與數字；本月支出在 MONEY 以純 SVG 灰階圓餅圖搭配分類金額與百分比呈現，不引入外部圖表套件。

為什麼：月支出占比適合視覺比較，七天生活紀錄則仍以逐日數字最清楚。

不這樣做會怎樣：只看分類金額較難快速辨認支出重心；引入套件又會增加離線快取與維護成本。

改動成本：要花點時間（需維護 SVG 與無資料狀態）。

### 23. 分類改為可維護清單並依使用次數排序
我決定了什麼：預設分類為吃飯、咖啡、加油、交通、衣物、生活、醫療、其他；分類可新增、改名與停用，並記住使用次數及最近一次金額，常用分類優先。

為什麼：常用項目與金額會隨實際生活形成，不應只靠固定預設或單一最近分類。

不這樣做會怎樣：快速記帳仍要反覆輸入分類與金額，難以維持兩動作的理想路徑。

改動成本：要花點時間（涉及分類資料、排序與舊資料相容）。

### 24. 記帳採支出、收入、移轉三種交易與三帳戶制
我決定了什麼：交易分為支出、收入、移轉；起手帳戶為主帳戶、現金、信用卡。支出依刷卡／現金／轉帳扣對應帳戶，收入加入指定帳戶，移轉一減一加且不計入月支出；卡費義務固定為主帳戶移轉至信用卡。

為什麼：繳卡費不是第二次支出，三種交易與帳戶餘額能避免月支出重複計算，也能清楚核對信用卡帳單。

不這樣做會怎樣：卡費與刷卡消費會重複計入，收入與資金搬移也無法和真正支出分開。

改動成本：難改（涉及 schema v2、升級、餘額、月結、匯出與 Undo 規則）。

### 25. 系統帳戶固定保留，自訂帳戶採封存

我決定了什麼：預設三個系統帳戶可改名，但不可刪除或停用；自訂帳戶採封存與解封，不永久刪除。封存帳戶不再供新交易選用，但歷史交易、餘額計算與匯出入永久保留。

為什麼：交易必須持續以穩定帳戶 ID 關聯；保留帳戶能避免舊交易因名稱變更或停止使用而失效。

不這樣做會怎樣：刪除或停用被歷史交易引用的帳戶，會讓餘額、匯出入與月結資料失去可追溯性。

改動成本：要花點時間（需同步限制系統帳戶、交易選單、餘額與匯出入規則）。

## 我沒有替你決定、需要你決定的事

### 是否要跨裝置同步
建議：先完成七天實測再決定。如果每天真的會同時用手機與電腦，再規劃同步；現在加入帳號或雲端會明顯增加複雜度與隱私風險。
